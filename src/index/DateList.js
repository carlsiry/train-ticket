import React from 'react'
import { memo } from 'react'

// 城市列表组件
function DateList(props) {
  //   const {} = props
  return <div className="date-list">DateList component works!</div>
}

export default memo(DateList)
