import React from 'react'
import { memo } from 'react'

// 城市列表组件
function CityList(props) {
  //   const {} = props
  return <div className="city-list">CityList component works!</div>
}

export default memo(CityList)
